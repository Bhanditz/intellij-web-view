<idea-plugin version="2">
    <name>Web View</name>
    <description>Displays project files in browser</description>
    <version>1.0</version>
    <vendor>JetBrains</vendor>
    <idea-version since-build="8000"/>

    <!-- Only for tests because flex plugin doesn't loaded wo this -->
    <depends optional="false">com.intellij.flex</depends>

    <application-components>
        <!-- Add your application components here -->
        <component>
            <implementation-class>web.view.ukhorskaya.IdeaHttpServer</implementation-class>
        </component>
    </application-components>

    <project-components>
        <!-- Add your project components here -->
    </project-components>

    <actions>
        <!-- Add your actions here -->
    </actions>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Add your extensions here -->
    </extensions>
</idea-plugin>